# MongoDB connection string (from MongoDB Atlas or local)
MONGODB_URI=mongodb+srv://akshatchaturvedi991_db_user:flaqxMjNTUCca1bW@cluster0.0bvnd7o.mongodb.net/?appName=Cluster0

# JWT secret — use a long random string in production
JWT_SECRET=hello_pumpkin_pumpkin

# JWT expiry
JWT_EXPIRES_IN=7d

# Client origin for CORS (no trailing slash)
CLIENT_URL=http://localhost:5173

# Server port
PORT=5000

# Node environment
NODE_ENV=development
